version: "3"
services:
  app:
    image: <your-docker-username>/note-taker-nodejs:latest
    container_name: note-taker
    env_file:
      - /home/ubuntu/envs/note-taker.env
    ports:
      - "3000:3000"
    restart: always
